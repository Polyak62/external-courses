function getType(type) {
    switch (typeof (x)) {
        case "number":
            return typeof x;
        case "string":
            return typeof x;
        default :
    }
}

var x = true;
console.log(getType(x));

